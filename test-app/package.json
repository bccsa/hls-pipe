{
  "name": "hls-pipe-test-app",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-driven test harness for hls-pipe. Express server proxies the library API to an ffplay subprocess.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
